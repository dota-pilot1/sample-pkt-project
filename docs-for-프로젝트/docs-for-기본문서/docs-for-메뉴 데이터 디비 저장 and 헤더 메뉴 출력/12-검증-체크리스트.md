# 스텝 12: 검증 체크리스트

> 현재 "메뉴 DB화 v1" 기준 검증 항목. 이 목록에서 ✅ 는 구현/검증 완료, ⚠️ 는 수동 검증만, ❌ 는 미구현.

---

## A. 백엔드

### A-1. 엔티티 & 스키마
- [x] `menus` 테이블 생성 (Hibernate `ddl-auto`).
- [x] `UNIQUE(code)` 제약 존재.
- [ ] `INDEX(parent_id, display_order)` — ❌ 미적용 (메뉴 수 적어 미도입).
- [ ] `ON DELETE RESTRICT` — ❌ 미적용. Hibernate 기본 FK.

### A-2. 시더
- [x] 빈 DB → 앱 기동 → 6개 row 시드.
- [x] 재기동 → row 수 그대로, 기존 값 보존 (`existsByCode` 스킵).
- [x] 시드에 새 `MenuDef` 1개 추가 + 재기동 → 새 row 만 insert.

### A-3. 조회 API
- [x] `GET /api/menus` 공개 (비로그인도 200).
- [x] 전체 반환 (숨김 포함).
- [ ] 서버측 role 필터 — ❌ 의도적 미구현 (프론트 책임).
- [ ] 고아 자식 제거 — ❌ 미구현 (프론트 optional chaining 으로 자연 처리).

### A-4. 변경 API
- [x] `POST /api/menus` (ADMIN) 생성.
- [x] `POST` + 중복 `code` → 409 `MENU_CODE_DUPLICATE`.
- [x] `POST` + 존재하지 않는 `parentId` → 404 `MENU_PARENT_NOT_FOUND`.
- [x] `PATCH /api/menus/{id}` (ADMIN) 수정.
- [x] `DELETE /api/menus/{id}` (ADMIN) 삭제.
- [ ] 자식 있는 메뉴 삭제 차단 — ❌ 선제 체크 없음. DB FK 위반 → 500 (향후 ErrorCode 추가 필요).
- [ ] 순환 참조 방어 — ❌ UI 제약(부모는 루트만)으로 우회.

### A-5. 권한 가드
- [x] 변경 API 비-ADMIN JWT → 403 `FORBIDDEN`.
- [x] 변경 API 미인증 → 401.

---

## B. 프론트 유틸

**단위 테스트 파일 없음** (`buildTree` 가 인라인이라 테스트 대상이 안 됨). 수동 검증만.

### B-1. `buildTree` (Header & MenuTreeTab)
- [x] ⚠️ 루트 2개 + 자식들 입력 → 트리 구성 확인 (수동).
- [x] ⚠️ `displayOrder` 정렬 동작 (수동).
- [ ] ❌ 같은 `displayOrder` 안정 정렬 — 미구현.
- [x] ⚠️ 부모 못 찾는 고아 row → 결과에서 드롭 (Optional chaining).

### B-2. `filterVisibleForUser` (Header 인라인)
- [x] ⚠️ `requiredRole=ROLE_ADMIN` + 유저 role 불일치 → 제외.
- [x] ⚠️ 부모 제외 시 자식 자동 제외 (전이).
- [x] ⚠️ `visible=false` → 제외.

### B-3. `pruneEmptyFolders` — ❌ 미구현
### B-4. `flattenTree` — ❌ 미구현

---

## C. 헤더 통합

### C-1. 로그인 상태별
- [x] 비로그인 → 메뉴 영역 빔, 로그인/회원가입 버튼.
- [x] 일반 유저(ROLE_USER) → "대시보드" 만.
- [x] 관리자(ROLE_ADMIN) → "대시보드" + "관리 ▾" + 자식 4개(유저/역할-권한/메인/메뉴 관리).

### C-2. 드롭다운 동작
- [x] 1차 "관리" 클릭 → 2차 드롭다운 열림.
- [x] 바깥 클릭 → 드롭다운 닫힘.
- [x] 경로 이동 → 드롭다운 자동 닫힘.
- [ ] ❌ 3차 서브 드롭다운 — `DropdownMenu` 가 평탄 렌더만 지원.

### C-3. Active 하이라이트
- [x] `/users` 등 진입 → "관리" 1차도 active 스타일 (1-depth 체크).
- [ ] ❌ 3차 이상 trail — 미지원.

### C-4. i18n / 아이콘
- [ ] ❌ i18n — `labelKey` 미해석, `label` 그대로.
- [ ] ❌ 아이콘 — 헤더에 아이콘 렌더 안 함.
- [x] `isExternal=true` → 새 탭 (2차 드롭다운 자식만).

---

## D. 관리 UI (`/menu-management`)

### D-1. CRUD
- [x] `+ 추가` 버튼 → 다이얼로그 → 생성.
- [x] `code` 필수 / 중복 시 toast 에러.
- [x] 좌측 트리 클릭 → 우측 DetailPanel 에 값 로드.
- [x] 수정 → 저장 → 헤더 즉시 반영 (invalidate).
- [x] 삭제 → `confirm` → 삭제.
- [ ] ⚠️ 자식 있는 메뉴 삭제 시도 → 서버 에러 toast (UI 사전 차단 없음).

### D-2. DnD
- [x] 같은 부모 안 드래그 → 순서 변경 → 개별 PATCH × N.
- [x] 드래그 중 시각 피드백 (opacity 0.4).
- [ ] ❌ 다른 부모로 이동 — 미지원.
- [ ] ❌ Optimistic rollback — 서버 실패 시 local 이 틀린 채로 남음 (다음 invalidate 로 복구).

### D-3. 상세 편집
- [x] 부모 메뉴 Select (루트만).
- [x] 표시 여부 toggle → 헤더에서 숨김.
- [x] `requiredRole` 변경 → 헤더 필터 반영.

---

## E. 회귀

- [x] `/dashboard` 접근 OK.
- [x] `/users`, `/role-permissions`, `/site-settings` 정상.
- [x] 로그인/로그아웃 사이클 정상.
- [x] 언어/테마 토글 정상.
- [x] 기존 `AdminDropdown` 하드코딩 제거됨.

---

## F. 최종 코드 정리

- [x] `Header.tsx` 에서 하드코딩 `AdminDropdown` / NavLink 목록 삭제됨.
- [x] `entities/menu` 디렉토리 생성됨.
- [x] `features/menu-management` 디렉토리 생성됨.
- [x] `MenuTable.tsx` 삭제됨 (리팩토링 후).
- [ ] ❌ `entities/menu/index.ts` barrel — 미작성.
- [ ] ⚠️ 프로젝트 README 에 "/menu-management" 언급 — 확인 필요.

---

## G. 스트레치 (v2)

- [ ] 서버측 유저별 권한 필터 옵션.
- [ ] Redis 메뉴 캐싱.
- [ ] 관리 UI 미리보기 모드 (role 시뮬레이션).
- [ ] 키보드 DnD.
- [ ] 다른 부모로 DnD 이동 + 순환 참조 방어.
- [ ] 3차 이상 메뉴 헤더 렌더 (재귀 DropdownMenu).
- [ ] i18n 해석 적용.
- [ ] 아이콘 렌더 (lucide 매핑).
- [ ] 자식 있는 메뉴 삭제 사전 차단 + `MENU_HAS_CHILDREN` 에러 코드.
- [ ] `/menu-management` 페이지에 ADMIN 가드.
- [ ] `@OneToMany children` + `JOIN FETCH` 최적화 (메뉴 수 늘어날 때).

---

## 마지막 점검 — 설계 원칙이 코드에 남아있는가

- [x] 서버는 트리 구성 코드를 **한 줄도** 갖지 않는다. 항상 플랫 JSON.
- [x] 프론트 `buildTree` 는 순수 함수. (다만 두 곳에 중복 존재 — 허용 범위)
- [x] 필터는 플랫에서 먼저 → 트리화.
- [x] 관리 UI 편집은 트리 뷰, 저장은 플랫 단위 API (개별 PATCH).
- [x] DB/JPA 엔티티에 `@OneToMany children` 없음.

**→ 5개 원칙 전부 유지됨. v1 완료.**
