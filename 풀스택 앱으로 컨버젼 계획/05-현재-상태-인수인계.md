# 현재 상태 및 다음 세션 인수인계

작성일: 2026-08-24

## 프로젝트 위치

```text
/Users/terecal/pilot-project/sample-pkt-project/pkt-study-fullstack
```

목표는 기존 Tauri + React/Vite + Spring Boot + PostgreSQL 앱을 Tauri + Next.js standalone + SQLite 단일 앱으로 전환하는 것이다.

## 완료된 기능

- Next.js 16, TypeScript, React 구성
- `better-sqlite3`와 Drizzle 기반 SQLite 연결
- Tauri 2 개발 실행 구조 및 `npm run tauri dev` 확인
- 세션 기반 로그인·로그아웃·인증 상태 조회
- 관리자 계정 로그인 확인
  - 이메일: `terecal@daum.net`
  - 개발용 비밀번호: `password123`
- 역할·사용자·권한·세션 테이블
- 플레이북 공간·카테고리·토픽·문서·댓글 구조
- 문서 생성·수정·저장 및 재시작 후 영속성 확인
- Lexical 에디터 및 실행 취소/다시 실행/서식/목록 기능
- 기존 비정상 Lexical JSON 정규화
- 문서 검색
- 댓글 등록·삭제
- 로그인 사용자만 문서·댓글 쓰기 가능
- 관리자만 백업 복구 가능
- SQLite 백업 다운로드 및 백업 파일 검증
- 앱 재시작 시 복구 파일 적용
- 백업 파일명 형식 개선
- PostgreSQL public 스키마 전체 export 도구 작성
- 핵심 계정·권한·플레이북 테이블의 SQLite import 검증 도구 작성
- source/target row count 및 SQLite foreign key 검증 도구 작성
- PostgreSQL 원본에만 있는 permission category·문서 토큰 컬럼을 SQLite에 반영
- PostgreSQL public 24개 테이블 전체를 별도 SQLite 검증 DB로 import
- ESLint 9용 프로젝트 설정 추가

```text
pkt-study-sqlite-backup-2026-08-24_07-13-44.db
```

## 주요 위치

```text
src/server/database.ts                       SQLite 초기화·시더·백업 상태
src/server/auth.ts                           세션·사용자·관리자 확인
src/server/playbook.ts                        플레이북 조회 로직
src/app/api/auth/                             로그인 API
src/app/api/hospital-playbook/                플레이북 API
src/app/api/backup/                           백업·복구 API
src/app/components/LoginPanel.tsx            로그인 UI
src/app/components/PlaybookPanel.tsx         플레이북 UI
src/app/components/LexicalDocumentEditor.tsx Lexical 에디터
src/app/components/BackupPanel.tsx            백업·복구 UI
src-tauri/                                    Tauri 설정
.data/pkt-study.db                            개발 DB
```

## 실행 및 검증

```bash
cd /Users/terecal/pilot-project/sample-pkt-project/pkt-study-fullstack
npm run dev
npm run tauri dev
npm run build
```

`npm run build`는 현재 통과한다. 개발 모드에서 로그인, 문서 편집, 저장, 백업 다운로드까지 확인했다.

## 아직 남은 핵심 작업

1. 전체 import 결과를 운영 전환용 파일로 보관하고 실제 사용자 데이터에 대한 최종 검토
2. generic으로 보존한 MES 테이블을 Drizzle 도메인 스키마와 API로 정식화
3. 권한 코드를 화면·API별로 세분화
4. Next standalone 서버를 Tauri sidecar로 포함하는 운영 빌드 완성
5. macOS 설치본 및 폐쇄망 환경 실행 검증
6. 기존 Tauri 핵심 화면을 기능별로 이전
7. 최종 사용자 데이터와 Lexical 문서 전체 마이그레이션

## 현재 판단

개발용 핵심 수직 기능과 PostgreSQL public 24개 테이블의 별도 SQLite import 검증은 완료 상태다. 실제 개발 DB는 덮어쓰지 않았고 `/tmp/pkt-study-import-check-v7.db`에서 row count와 foreign key 검증을 통과했다. generic으로 생성된 MES 테이블은 데이터 보존 단계가 완료된 상태이며, 다음은 이를 화면·API 도메인으로 정식화하는 작업이다. 실행 절차와 매핑은 `pkt-study-fullstack/docs/postgres-sqlite-mapping.md`에 기록했다.
